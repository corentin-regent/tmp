name: Synchronize Poetrel labels

on:
  schedule:
    - cron: '* * */1 * *'  # once a day
  push:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Sync labels with GitHub
        uses: corentin-regent/poetrel/sync-labels@develop
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          owner: corentin-regent
